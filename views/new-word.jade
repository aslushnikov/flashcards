extends layout

block content
  .title
    a(href="/") #{user.email}
  form.word-block(id="create-word", method="POST", action="/word/new")
    textarea(type="text", name="original", placeholder="hablar")
    textarea(type="text", name="translation", placeholder="speak")
    .button.submit-word

